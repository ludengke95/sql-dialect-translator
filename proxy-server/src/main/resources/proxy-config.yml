# MySQL Proxy 默认配置
proxy:
  port: 3306
  auth:
    user: root
    password: proxy_password

target:
  dialect: POSTGRESQL
  jdbc-url: jdbc:postgresql://localhost:5432/mydb
  username: pg_user
  password: pg_password
  max-pool-size: 20
  min-idle: 2
